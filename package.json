{
  "name": "@appneural/data-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Data Studio — served at data.studio.appneural.com",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=20"
  }
}
